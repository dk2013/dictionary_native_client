import React from 'react';
import { Text } from 'react-native';
import PageTemplate from 'PageTemplate';

const AddWord = () => {
  return (
    <PageTemplate title="AddWord">
      <Text>AddWord</Text>
    </PageTemplate>
  );
};

export default AddWord;
